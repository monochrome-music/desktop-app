/// Android-specific setup.
///
/// Future additions: notification media controls, picture-in-picture,
/// Android Auto integration, etc.
pub fn setup(_app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
